export const signup = (req, resp) => {
  resp.send("signup routes");
};

export const login = (req, resp) => {
  resp.send("login routes");
};

export const logout = (req, resp) => {
  resp.send("logout routes");
};
